# Corrections

## Обязательно доделать

- [x] Добавить в таблицу все поля `HumanBeing`.
  Сейчас в таблице есть только `id`, `key`, `name`, `x`, `y`, `impactSpeed`, `mood`, `owner`.
  Нужно добавить `creationDate`, `realHero`, `hasToothpick`, `soundtrackName`,
  `minutesOfWaiting`, `car`.

- [ ] Выводить дату создания по активной локали.
  `LocalizedFormatter` уже умеет форматировать даты, но `creationDate` почти нигде не
  отображается. После добавления колонки нужно форматировать дату через него.

- [ ] Исправить ввод и сохранение `hasToothpick`.
  Сейчас чекбокс сохраняет только `true` или `null`, хотя поле модели допускает
  `true`, `false`, `null`. Лучше заменить на `ComboBox`: `Да`, `Нет`, `-`.

- [ ] Расширить popup объекта.
  Сейчас popup показывает не все поля. Нужно добавить хотя бы дату создания,
  саундтрек, минуты ожидания и зубочистку, чтобы клик по объекту показывал
  полную информацию.

- [ ] Проверить редактирование всех полей.
  После исправления `hasToothpick` нужно убедиться, что все поля корректно
  сохраняются, обновляются в таблице и синхронизируются у других клиентов.

## Желательно для сдачи

- [ ] Обновить `plan.md`.
  Сейчас чеклист в конце файла весь не отмечен, хотя большая часть проекта уже
  реализована. Нужно отметить выполненные пункты и оставить незавершённые.

- [ ] Обновить `README.md` под реальный запуск через Helios.
  Нужно добавить короткую инструкцию: сервер запускается на Helios, локально
  поднимается SSH-туннель `ssh -L 1111:localhost:1111`, клиент запускается на
  локальном компьютере и подключается к `localhost:1111`.

- [ ] Убрать или явно описать ограничение GUI-скриптов.
  Сейчас GUI выполняет только команды без полноценного ввода объекта. Если
  преподаватель строго проверяет поддержку всех команд предыдущих лабораторных,
  это может быть проблемой. Минимум нужно описать ограничение, максимум -
  добавить поддержку `insert`, `update`, `remove_lower` из скрипта.

- [ ] Проверить команды тулбара на соответствие Lab7.
  Особенно `remove_lower`: сейчас GUI спрашивает только имя и создаёт dummy-объект,
  хотя серверная команда сравнивает полноценный `HumanBeing`.

- [ ] Провести ручной сценарий проверки.
  Сценарий: сервер на Helios, SSH-туннель, два локальных клиента, два пользователя,
  добавление, изменение, удаление, смена языка, popup, автообновление.

## Технические риски

- [ ] Проверить сетевой протокол без prefixed length.
  `RequestSender` и `ResponseReceiver` передают сериализованный объект без явной
  длины сообщения. Сейчас это может работать, но через SSH-туннель возможны
  таймауты или неполное чтение больших ответов.

- [ ] Учитывать обрыв SSH-туннеля.
  Клиент умеет переподключаться к `localhost:1111`, но если умер сам SSH-туннель,
  переподключение не поможет. Для сдачи нужно держать туннель открытым в отдельном
  терминале.
